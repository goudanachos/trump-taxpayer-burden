---
layout: layouts/base.liquid
title: Expenditure Archive
description: Complete archive of daily taxpayer burden expenditures
---

<!-- CLASSIFICATION HEADER ---->
<section style="background: var(--color-bg-command); border-bottom: var(--border-authority); padding: var(--space-xl) var(--space-lg);">
    <div class="max-w-4xl mx-auto text-center">
        <!-- Classification Header -->
        <div class="authority-font" style="color: var(--color-text-inverse); font-size: var(--text-detail); margin-bottom: var(--space-sm); opacity: 0.8; letter-spacing: 0.1em;">
            /// CLASSIFIED: COMPLETE EXPENDITURE ARCHIVE ///
        </div>
        
        <!-- Page Title -->
        <h1 class="command-font" style="color: var(--color-text-inverse); font-size: var(--text-command); margin-bottom: var(--space-md); text-transform: uppercase; letter-spacing: 0.05em;">
            TAXPAYER BURDEN ARCHIVE
        </h1>
        
        <!-- Subtitle -->
        <div class="standard-font" style="color: var(--color-text-inverse); font-size: var(--text-standard); opacity: 0.8;">
            Comprehensive record of recreational and leisure expenditures
        </div>
        
        <!-- Authority Stamp -->
        <div class="standard-font" style="color: var(--color-text-inverse); font-size: var(--text-detail); margin-top: var(--space-lg); opacity: 0.6; text-transform: uppercase; letter-spacing: 0.1em;">
            FOR OFFICIAL USE ONLY // AUTHORIZED PERSONNEL
        </div>
    </div>
</section>

<!-- ARCHIVE STATISTICS -->
<div class="max-w-4xl mx-auto px-4" style="padding: var(--space-xl) 0; border-bottom: var(--border-authority); margin-bottom: var(--space-xxl);">
    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: var(--space-lg); text-align: center;">
        <!-- Total Records -->
        <div style="background: var(--color-bg-secondary); border: var(--border-authority); padding: var(--space-lg);">
            <div class="classification-font" style="color: var(--color-text-alert); font-size: clamp(1.5rem, 4vw, var(--text-command)); font-weight: 700; margin-bottom: var(--space-sm); white-space: nowrap;">
                {{ collections.expenditures.size }}
            </div>
            <div class="standard-font" style="color: var(--color-text-primary); font-size: var(--text-detail); text-transform: uppercase; letter-spacing: 0.05em;">
                TOTAL RECORDS
            </div>
        </div>
        
        <!-- Total Cost -->
        <div style="background: var(--color-bg-secondary); border: var(--border-authority); padding: var(--space-lg);">
            {% assign total_cost = 0 %}
            {% for expenditure_data in collections.expenditures %}
                {% assign expenditure = expenditure_data.data %}
                {% assign total_cost = total_cost | plus: expenditure.total_cost %}
            {% endfor %}
            <div class="classification-font" style="color: var(--color-text-alert); font-size: clamp(1.5rem, 4vw, var(--text-command)); font-weight: 700; margin-bottom: var(--space-sm); white-space: nowrap;">
                {{ total_cost | currency }}
            </div>
            <div class="standard-font" style="color: var(--color-text-primary); font-size: var(--text-detail); text-transform: uppercase; letter-spacing: 0.05em;">
                TOTAL EXPENDITURE
            </div>
        </div>
        
        <!-- Highest Daily Cost -->
        <div style="background: var(--color-bg-secondary); border: var(--border-authority); padding: var(--space-lg);">
            {% assign highest_expenditure = collections.expenditures | map: 'data' | sort: 'total_cost' | reverse | first %}
            <div class="classification-font" style="color: var(--color-text-alert); font-size: clamp(1.5rem, 4vw, var(--text-command)); font-weight: 700; margin-bottom: var(--space-sm); white-space: nowrap;">
                {{ highest_expenditure.total_cost | currency }}
            </div>
            <a href="/day/{{ highest_expenditure.date | date: '%Y/%m/%d' }}/" class="standard-font" style="color: var(--color-text-primary); font-size: var(--text-detail); text-transform: uppercase; letter-spacing: 0.05em; text-decoration: none; display: block;">
                HIGHEST DAILY COST
                <div style="font-size: 0.9em; opacity: 0.7; margin-top: 4px;">{{ highest_expenditure.date | date: '%B %-d, %Y' }}</div>
            </a>
        </div>
    </div>
</div>

<!-- COMPLETE EXPENDITURE ARCHIVE -->
<div class="max-w-4xl mx-auto px-4" style="padding-bottom: var(--space-xxl);">
    <!-- Archive Grid -->
    <section>
        {% for expenditure_data in collections.expenditures %}
            {% assign expenditure = expenditure_data.data %}
            {% include "components/expenditure-card.liquid", expenditure: expenditure %}
        {% endfor %}
    </section>
    
    <!-- Classification Footer -->
    <div style="text-align: center; margin-top: var(--space-xxl); padding-top: var(--space-xl); border-top: var(--border-authority);">
        <div class="standard-font" style="color: var(--color-text-secondary); font-size: var(--text-detail); text-transform: uppercase; letter-spacing: 0.1em; line-height: 1.6;">
            END OF CLASSIFIED ARCHIVE<br>
            AUTHORIZED FOR PUBLIC DISCLOSURE UNDER FOIA
        </div>
    </div>
</div>